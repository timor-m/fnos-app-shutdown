# Changelog

## 1.0.3

- 优化部署与验证命令展示，使用临时变量减少重复路径
- 部署验证完成后立即刷新执行器状态，无需等待下一次 cron
- 修复执行器状态目录权限导致的部署状态误报，并暴露状态读取错误

## 1.0.2

- 安全修复：直连服务端口仅绑定 `127.0.0.1`，不再允许局域网绕过 fnOS 网关访问应用

## 1.0.1

- 修复执行器默认数据目录硬编码 `/vol1`：部署命令把应用实际 `STORAGE_DIR` 显式写入 cron；不再以 `/var/apps` 或任何猜测路径兜底，目录缺失或无效时安全退出
- 部署命令通过 `sudo setcap cap_net_raw+ep "$(command -v ping)"` 为系统 ping 授权，修复低权限应用用户执行「主机在线」检测返回 rc=2
- 设置页工作阶段改用原生时间选择器，iOS/iPadOS 使用系统滚轮交互

## 1.0.0

- 首个正式版：夜间工作阶段 + 15 项空闲检查项（逐项开关/阈值可配），全部通过自动关机
- 执行器签名自更新：应用升级后 ≤10 分钟自动验签同步，无需手动操作
- 部署向导（一键部署 + 三态健康检测）、手动检测、今晚跳过
- 运行日志按「次」分组、日期筛选、按月滚动保留 6 个月
- 明亮/暗黑双主题、图标化高密度界面、桌面入口对所有用户开放、可选直连端口

## 0.5.1

- 运行日志保留策略：仅保留最近 6 个月（含当月），执行器每次触发自动清理更旧月份并记录清理日志（契约 §3.4）

## 0.5.0

- 执行器签名自更新（契约 §3.6）：cron 触发时对比包内脚本版本，RSA-3072/SHA-256 验签通过后原子替换自身并重新执行；验签失败记日志继续旧版——应用升级后无需再手动跑部署命令
- 信任锚为首次 sudo 部署锚定的内嵌公钥；签名私钥离线保管（`keys/`，不入库）
- 部署页一键命令调整为「首次部署 / 手动修复」定位

## 0.4.3

- 应用介绍补全核心功能（15 项检查项、部署向导、运行日志、今晚跳过、双主题、直连端口等），统一「工作阶段」措辞
- 日志页：分组常显（不再折叠）、组间留白分隔、明细行只显示时分秒、面板内边距收紧

## 0.4.2

- 关于页应用信息新增「作者」（链接到维护者主页）；manifest maintainer/distributor 落实为 timor-m

## 0.4.1

- 部署页一键命令改用 127.0.0.1 + 应用直连端口（免网关注销态、避免域名回环失败）；未启用直连端口时给出提示
- 顶栏吸顶、主题切换固定右上；关于页负载恢复 1/5/15 分钟拆分展示

## 0.4.0

- 桌面入口对所有用户开放（allUsers，访问权限可编辑）
- 新增可选直连服务端口：安装/设置向导可配置（默认 8366，0 = 关闭），与统一网关双通道监听
- 关于页展示直连端口状态
- fpk 产物文件名带版本号（fnos-app-shutdown-x.y.z.fpk）

## 0.3.2

- 运行日志按「次」分组折叠展示（契约 §3.4 分隔标记 `=== 触发执行（vX.Y.Z）===`），倒序排列、吸顶组头
- 状态页「今晚跳过」在总开关关闭时显示「已暂停」，消除歧义
- 新应用图标（夜景靛蓝渐变 + 电源符号），页面头部改版
- 日志区加宽、时间戳弱化等界面密度优化

## 0.3.0

- 检查项扩至 15 项：新增虚拟机运行、进程守护、磁盘擦洗、主机在线、日历规则、SMB 会话、TCP 会话、下载连接、最小运行时长、网络 TX
- 状态页手动检测（executor --dry-run 零副作用）
- 明亮/暗黑双主题、导航图标化、关于页（应用/系统信息）
- 部署向导三态健康检测与一键部署命令

## 0.1.0

- 首个可用版本：工作阶段 + 6 项基础检查（CPU、负载、在线用户、SSH、磁盘 IO、网络）
- root 执行器与应用的文件系统协作契约 v1（config/status/skip/log）
- 统一网关接入、Unix Socket 运行态、最小权限 run-as=package
- fpk 打包、包结构校验与 GitHub Actions CI/Release
